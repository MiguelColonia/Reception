

@page "/Parkplatz"
@using System.Net.Mail

<style>
    .container {
        max-width: 400px;
        margin: 0 auto;
        padding: 20px;
        background-color: #f8f9fa;
        border-radius: 5px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .input-container {
        margin-bottom: 20px;
    }

    label {
        display: block;
        font-weight: bold;
        margin-bottom: 5px;
    }

    textarea {
        width: 100%;
        height: 100px;
        padding: 10px;
        margin-bottom: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-sizing: border-box;
    }

    .button {
        display: block;
        width: 100%;
        padding: 10px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
        text-align: center;
    }

        .button:hover {
            background-color: #0056b3;
        }
</style>

<div class="container">
    <h3>Parkplatz</h3>
    <div class="input-container">
        <label for="parkplatzInfo">Schreiben Sie hier alles rund um die Parkplätze:</label>
        <textarea id="parkplatzInfo" @bind="parkplatzInformation" placeholder="Hier eingeben..."></textarea>
    </div>
    <button class="button" @onclick="Speichern">Speichern</button>
</div>

@code {
    private string parkplatzInformation = "";

    private void Speichern()
    {
        // Hier können Sie den Code zum Speichern und Senden der Informationen implementieren
        // Zum Beispiel:
        // - Speichern der Informationen in einer Datenbank
        // - Senden der Informationen per E-Mail

        // Nachdem die Informationen gespeichert und gesendet wurden, können Sie das Textfeld leeren
        parkplatzInformation = "";

        // Hier können Sie den Code zum Senden der E-Mail einfügen
        // Zum Beispiel:
        /*
        var client = new SmtpClient("smtp.example.com")
        {
            Port = 587,
            Credentials = new NetworkCredential("username", "password"),
            EnableSsl = true,
        };

        client.Send("from@example.com", "to@example.com", "Betreff", parkplatzInformation);
        */
    }
}
